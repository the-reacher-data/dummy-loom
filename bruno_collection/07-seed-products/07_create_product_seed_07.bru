meta {
  name: seed_create_product_07
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/products/
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "sku": "SEED-SKU-007",
    "name": "Seed Router",
    "category": "network",
    "price_cents": 14900,
    "stock": 3
  }
}

tests {
  test("status is 201", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
