meta {
  name: flow_delete_address
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/users/{{userId}}/addresses/{{addressId}}
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
