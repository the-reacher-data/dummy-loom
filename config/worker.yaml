includes:
  - base.yaml
  - celery.yaml

jobs:
  SendRestockEmailJob:
    queue: notifications
    retries: 2
  BuildProductSummaryJob:
    queue: analytics
    retries: 1
  SyncProductToErpJob:
    queue: erp
    retries: 3
